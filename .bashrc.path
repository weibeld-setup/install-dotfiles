# ~/.bashrc.path
#
# PATH setup
#------------------------------------------------------------------------------#

# TODO: add to .bashrc

# Can't use 'brew --prefix' because 'brew' is not yet in the PATH
_path-prepend /opt/homebrew/bin
#_path-append "$HOME"/google-cloud-sdk/bin

# Delete duplicate and invalid PATH entries (non-existing directories)
_path-uniq
_path-rectify
